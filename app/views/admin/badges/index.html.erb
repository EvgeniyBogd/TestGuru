<h1> <%= t('.header')%> </h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col"><%= t('.title') %></th>
      <th scope="col"><%= t('.image') %></th>
      <th scope="col"><%= t('.discription') %></th>
      <th scope="col"><%= t('.rule') %></th>
      <th scope="col"><%= t('.rule_value') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @badges %>
  </tbody>
</table>    

<%= link_to "New badge", new_admin_badge_path %>
