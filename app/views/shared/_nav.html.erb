<nav class="navbar navbar-light bg-light">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if user_signed_in? %>
      Welcome, <b><%= current_user.name %></b> Guru |
      <%= link_to 'Admin Panel', admin_tests_path if current_user.admin? %>
      <%= link_to 'Gists', admin_gists_path if current_user.admin? %>
      <%= link_to 'Feedbacks', admin_feedbacks_path if current_user.admin? %>
      <%= link_to 'Feedback', new_feedback_path unless current_user.admin? %>
      <%= link_to 'Badges', badges_path unless current_user.admin? %>
      <%= link_to 'My Badges', user_badges_path unless current_user.admin? %>
      <%= link_to 'Badges', admin_badges_path if current_user.admin? %>
      <%= link_to 'New Badge', new_admin_badge_path if current_user.admin? %>
      <%= link_to 'Log out', destroy_user_session_path, data: {turbo_method: :delete} %></p>

    <% else %>
      <%= link_to 'Sign up', new_user_registration_path %> |
      <%= link_to 'Log in', new_user_session_path %>
    <% end %>  
  </span>
</nav>  